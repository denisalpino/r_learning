
# rm - функция, которая удаляет переменные
# function_name <- function(x, y, ...) {
#   name_of_object <- some operations
#   return(name_of_object)
# } - данная функция обладает сторонним эффектом, так как принимает значение
# из-вне и преобразовывает его
# function_name <- function() {
#   name_of_object <- some operations
#   return(name_of_object)
# } - данная функция называется обкновенной, так как является постоянной и
# и всегда возвращает одинаковое значение
